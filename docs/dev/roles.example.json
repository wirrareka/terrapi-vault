{
  "_comment": "Example VAULT_ROLES_CONFIG for the EU broker. Maps each client cert's first SAN dNSName to a role + its granted capabilities (least privilege). Capabilities: ssh-ca, ssh-sign, creds, session, leases, kms. An unmapped/trusted SAN gets 403; a cap the role lacks gets 403.",
  "roles": {
    "demon-operator.eu.proximi.internal": {
      "role": "demon-operator",
      "caps": ["ssh-sign", "creds", "session", "leases"]
    },
    "demon-system.eu.proximi.internal": {
      "role": "demon-system",
      "caps": ["ssh-sign", "session", "leases"]
    },
    "aether-backup.eu.proximi.internal": {
      "role": "aether-backup",
      "caps": ["kms"]
    }
  }
}
